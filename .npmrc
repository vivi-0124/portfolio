# pnpmの設定ファイル

# 厳格なピア依存関係チェックを無効にする
strict-peer-dependencies=false

# 自動インストールのピア依存関係を有効にする
auto-install-peers=true

# ホイスティングを有効にする（依存関係の問題を解決するため）
shamefully-hoist=true

# 依存関係の解決モード
resolution-mode=highest

# npmレジストリを使用
registry=https://registry.npmjs.org/

# node-linkerをhoistに設定
node-linker=hoisted 